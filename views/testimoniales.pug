extends ./layout/index

block contenido
    main.container.mt-5 
        h1.text-center=pagina
        include /layout/bloque-testimoniales

        .row.justify-content-center

            .col-md-12 
                h2.d-block.w-100.text-center Agrega un testimonial

                if(errores)
                    each error in errores 
                        .alert.col.alert-danger.text-center= error.mensaje
            
            .row
                .col-md-8 
                form(action="/testimoniales", method="POST")
                    .form-group 
                        label(for="nombre") Nombre 
                        input#nombre(type="text", placeholder="Tu nombre", name="nombre", class="form-control", value=nombre)
                    .form-group 
                        label(for="correo") Correo electrónico 
                        input#correo(type="mail", placeholder="Tu correo", name="correo", class="form-control", value=correo)
                    .form-group 
                        label(for="mensaje") Mensaje 
                        textarea#mensaje(class="form-control", name="mensaje", placeholder="Escribe un mensaje", rows= 3)=mensaje
                    input.btn.btn-success.btn-block(type="submit")